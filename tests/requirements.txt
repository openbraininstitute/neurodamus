matplotlib
# pytest-isolate-mpi cann't pickle tmp_path_factory from pytest-9.1.0 because of an unpicklable _BaseExitStack
pytest<9.1.0
pytest-forked
pandas
# 3.15.0 breaks compatibility with older macOS versions:
# https://github.com/h5py/h5py/blob/master/docs/whatsnew/3.15.rst
# Thus, we avoid that wheel
h5py!=3.15.0
